# Contributing

提交前运行 `moon fmt --check`、native/wasm-gc 的 check 与 test。行为修复必须包含能在修复前失败的测试；新增数据库适配器不得把驱动依赖引入核心包，也不得拼接未转义 SQL 值。

提交信息应描述一个可审查的功能或材料变化。禁止用空提交、重复文件、生成物或机械拆分满足提交数或源码量要求；`_build`、测试、示例和文档均不计入生产源码统计。
