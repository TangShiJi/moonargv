# 维护计划

- **兼容承诺：** 按语义化版本管理公共 API；0.x 阶段的破坏性变更写入 CHANGELOG 并提供迁移说明，1.0 后仅在主版本调整接口。
- **持续验证：** 每次提交在 Windows/Ubuntu 上执行 native 与 wasm-gc 构建、66+ 项单元/集成测试、微软 CRT 对照向量和 1,500 组确定性模糊样本。
- **问题处理：** 每周至少一次整理 issue；解析错误、参数边界破坏或资源上限绕过按高优先级处理，先增加最小回归用例再修复。
- **性能维护：** 版本发布前运行固定 release 基准；同环境中位数回退超过 20% 时记录分析与处理结论。
- **规则跟踪：** 每季度复核 POSIX 与 Microsoft CRT 文档；规则差异以显式方言或版本化行为处理，不静默改变既有结果。
- **近期路线：** 0.2 完善响应文件编码/BOM 策略和更多编译器约定；0.3 提供常用进程库薄适配层；1.0 固化错误模型与兼容矩阵。项目仍不扩展为 shell 或重复实现 `argparse`。
