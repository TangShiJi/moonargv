# 维护计划

- 每次提交在 Windows/Ubuntu 上验证 native 与 wasm-gc，任何 P0 行为变化必须增加回归测试；
- 0.x 阶段可扩展游标版本，但解码器必须明确拒绝未知版本，不静默猜测；
- 新增键类型、collation 或数据库适配器时，先更新兼容矩阵和差分用例；
- 每次发布重新在 mooncakes.io 检查同类包，并在 `docs/ecosystem-audit.md` 记录日期、查询词和差异；
- 后续优先级：HMAC 扩展接口、PostgreSQL/SQLite 参数化适配示例、属性/模糊测试、百万级索引查询基准；这些不冒充当前 MVP 已完成能力。

兼容性或安全缺陷应附最小复现、游标版本、排序声明、目标后端及 MoonBit 版本。维护者先添加失败测试，再修正实现和文档。
