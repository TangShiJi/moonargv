# MoonPageFlow 项目申报书

**基本信息**

- 项目名称：MoonPageFlow——稳定游标分页与可恢复遍历引擎
- 参赛者：唐仕吉
- 联系方式：t1622051617@qq.com / 13384247418
- GitHub 仓库链接：https://github.com/TangShiJi/moonpageflow
- 项目方向：MoonBit Web/API 与数据访问基础设施
- 项目性质：原创项目（非移植，MIT License）

**项目简介、价值与通用性**

动态列表使用 `offset/limit` 时，若两次请求之间有记录插入或删除，边界会移动，产生重复或漏读；深 offset 还会迫使数据库跳过大量记录。业务自行拼接游标又容易遗漏唯一键、NULL 次序、版本和快照，使问题只在并发或同值排序时出现。MoonPageFlow 提供数据库、框架和传输格式无关的稳定游标契约：以“业务排序键 + 唯一 ID”建立全序，把位置编码为不透明游标，并将边界转换成可供 SQL/ORM 适配器消费的 seek 计划。它不是数组切片工具，也不取代 ORM；它补的是各数据层都需要、但不应各自重复实现的分页语义层。

截至 2026-09-17，我核查了 mooncakes.io 的 2,504 个模块及 cursor/keyset/seek/page token/pagination 等关键词。`oboard/morm` 提供绑定 ORM 的 offset 页码与计数，`moonbitstack/moondb` 的 Cursor 是单连接流式取行，`marianoguerra/slack` 的游标分页是 Slack SDK 内部协议；均未提供独立的复合 keyset、双向游标、快照和恢复检查点引擎。`bobzhang/pagelayout` 是文档页面布局，与本项目无关。

**完整使用场景**

1. 活动流/API：按 `created_at DESC` 返回部署记录，同秒记录由 ID 稳定定序；请求下一页前即使有新事件插入，也不重复已读记录，快照变化可选择明确拒绝。
2. 管理后台/GraphQL 连接：在搜索结果或工单表格中提供 `first/after`、`last/before`、起止游标及前后页标志，同一契约可由 HTTP JSON 或 GraphQL resolver 封装。
3. 数据库与 ORM 适配：把 `(created_at DESC, priority ASC, id ASC)` 的游标编译成等值前缀加比较分支，适配器映射为参数化 SQL/查询构造器并命中复合索引，无需本库绑定某一驱动。
4. 批量导出/增量扫描：每批保存游标、快照、页数和已见 ID；进程中断后恢复，遇到游标不前进、记录重复或数据版本漂移立即报错，避免静默生成残缺文件。

**核心功能、实现路径与工程边界**

- Int64/String/Bool/Null 复合排序，升降序、显式 NULL 位置及唯一 ID 兜底；版本化 URL-safe 游标携带类型、排序元数据、快照和误改校验；
- 双向排页、PageInfo、大小/字段数/游标长度/输入行数上限；抽象 seek 计划；可恢复遍历检查点；offset 兼容页与重复、遗漏、乱序审计；
- 核心实现采用字典序逐键比较和排他边界；数据库适配层按 `(k1>v1) OR (k1=v1 AND k2>v2)…` 生成条件，反向请求反转比较与查询顺序再恢复展示顺序；宿主提供事务版本、事件偏移或内容哈希作为快照；
- 明确不做数据库连接、SQL 解析、ORM/GraphQL 服务器、网络/文件 I/O，也不把非密码学校验和宣传为签名。MVP 已有 1,608 行有效核心 MoonBit 源码、51 项原生与 wasm-gc 测试、数据库适配结果校验器及三个下游示例。

**可直接验收的行为标准**

同值排序必须按唯一 ID 稳定；游标往返保留 Unicode/NULL/方向并拒绝损坏或排序不匹配；五条基线记录在首屏后插入更靠前记录时，offset 对照出现重复/漏读，而 keyset 连续结果仍覆盖原基线且无重复；正反向页对称；快照不一致、重复游标和超限请求返回结构化错误；原生与 wasm-gc 全部测试、Windows/Ubuntu CI 和三个示例均通过。

**原创与提交说明**

本项目为原创实现，未移植或复制第三方源码；仅采用公开的 keyset pagination、GraphQL Connection 字段命名等通行概念。旧命令行选题已撤销，其源码已从当前树删除且不计入上述行数。当前仓库已有超过 10 个有效提交；本次换题按数据模型、游标编解码、分页/seek/恢复、漂移审计、下游场景与文档分别形成可独立审查的实现提交，不以空提交或机械拆分凑数。
